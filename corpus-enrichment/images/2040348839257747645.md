# @DeRonin_ — https://x.com/DeRonin_/status/2040348839257747645

**Transcription:**

README

# AutoAgent

Like autoresearch but for agent engineering. Give an AI agent a task, let it build and iterate on an agent harness autonomously overnight. It modifies the system prompt, tools, agent configuration, and orchestration, runs the benchmark, checks the score, keeps or discards the change, and repeats.

**AutoAgent Progress: SpreadSheetBench and TerminalBench**

[THIS IS CHART: A line graph showing performance over experiment iterations (0-30 on x-axis, 20%-100% score on y-axis). Two sets of lines are shown: green lines for "SpreadSheet" benchmarks (running_best, kept, and misclicked) trending upward from ~40% to ~100%, and blue lines for "Terminal" benchmarks (running_best, kept, and misclicked) trending upward from ~25% to ~55%.]

Legend:
- SpreadSheet — running_best
- SpreadSheet — kept
- SpreadSheet — misclicked
- Terminal — running_best
- Terminal — kept
- Terminal — misclicked

The core idea is the same: you're not touching the harness Python files like you normally would as an engineer. Instead, you program `program.md`, the Markdown file that provides context to the meta-agent and defines the agent-engineering loop.

## How it works

**Description:** This is a GitHub README screenshot showing the AutoAgent project, which is an autonomous agent improvement system that iteratively optimizes AI agents without human intervention. The main visual element is a progress chart demonstrating performance improvements on two benchmarks (SpreadSheetBench and TerminalBench) over 30+ experiment iterations.
