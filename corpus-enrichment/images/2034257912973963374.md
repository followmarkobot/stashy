# @stripe — https://x.com/stripe/status/2034257912973963374

**Transcription:**
```
const paymentIntent = await stripe.paymentIntents.create({
  amount: 1,
  currency: 'usd',
  payment_method_types: ['crypto'],
  payment_method_data: {
    type: 'crypto',
  },
  payment_method_options: {
    crypto: {
      mode: 'deposit',
      deposit_options: {
        networks: ['tempo'],
      },
    },
  },
  confirm: true,
});
```

**Description:** A code screenshot showing a Stripe API call example (JavaScript/Node.js) creating a payment intent that accepts crypto payments via deposit mode on the "tempo" network.
